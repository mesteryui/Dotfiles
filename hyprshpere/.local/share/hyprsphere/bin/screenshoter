#!/bin/bash

if [[ "$1" == "selection" ]]; then
    grim -g "$(slurp)" - | satty --filename - --output-filename "$HYPRSHOT_DIR/satty-annotated-$(date +'%Y-%m-%d-%H%M%S').png"
else
grim - | satty --filename - --output-filename "$HYPRSHOT_DIR/satty-annotated-$(date +'%Y-%m-%d-%H%M%S').png"
fi
